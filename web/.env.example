# Leave empty when using Flask integrated mode (http://localhost:5000 serves UI + API).
# For Vite dev only (npm run dev): leave empty — uses proxy /api → Flask :5000
# VITE_API_BASE_URL=

# Phone on same Wi-Fi (Flask on PC IP):
# VITE_API_BASE_URL=http://192.168.1.10:5000
